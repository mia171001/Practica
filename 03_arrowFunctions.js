//Before
function subtract(a, b) {
    return a - b;
}

//After

const subtractN = (a, b) => a - b;

//las  arrow function proporcionan una forma más concisa y menos propensa a errores de escribir funciones en JavaScript, especialmente para funciones simples y expresivas. Son ampliamente utilizadas en el desarrollo moderno de JavaScript.